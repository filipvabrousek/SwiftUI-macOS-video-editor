import SwiftUI

@main
struct claude_macOS_video_editorApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        .windowStyle(.titleBar)
        .defaultSize(width: 1200, height: 700)
    }
}
